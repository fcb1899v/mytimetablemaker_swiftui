# Uncomment the next line to define a global platform for your project
# platform :ios, '10.0'

target 'mytimetablemaker_swiftui' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for mytimetablemaker_swiftui
  pod 'FirebaseUI/Auth'
  pod 'FirebaseUI/Email'
  pod 'Firebase/Firestore'
  pod 'Google-Mobile-Ads-SDK'

  target 'mytimetablemaker_swiftuiTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'mytimetablemaker_swiftuiUITests' do
    # Pods for testing
  end

end
